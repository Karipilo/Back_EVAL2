services:

  backend:
    build: .
    container_name: backend_proyecto
    restart: always

    environment:
      DB_HOST: 10.0.143.151
      DB_PORT: 3306
      DB_USER: app_user
      DB_PASSWORD: DevOps123!
      DB_NAME: proyecto_db

    ports:
      - "3000:3000"

    networks:
      - app_network

networks:
  app_network: